// Remove this line:
import Link from 'next/link'

// Keep the rest of the file as is:
import { Building } from 'lucide-react'

export function Footer() {
  return (
    <footer className="bg-gray-900 text-white py-12">
      {/* rest of your footer code stays the same */}
    </footer>
  )
}